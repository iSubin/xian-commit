# 项目基线

本文件由 Document Render Pipeline 生成。机器事实源包括项目 marker 检查和 `docs/xian-harness/project-risks.json`；不要手工编辑本文件来改变基线风险。

## Identity

- Recommended profile: base
- Source control: git

## Runtime

| Runtime | Status | Evidence |
|---|---|---|
| none | missing | marker inspection |

## Present Markers

| Path | Type | Status |
|---|---|---|
| none | none | missing |

## Project Risks

| Severity | Risk | Impact | Mitigation |
|---|---|---|---|
| P1 | Git hooks can be silently bypassed by path-resolution mistakes | Sensitive-path and commit-message guardrails may not run. | Use Git-resolved shared hook paths, reject core.hooksPath, parse staged names as NUL-delimited data, and retain regression coverage. |
| P2 | External hook managers require manual integration | A user may assume both hook sets execute when only one owner is active. | Fail fast on core.hooksPath and document ownership, backup, and manual chaining behavior. |

## Guardrails

- 先判断目标项目，再使用 profile 规则
- 不使用未验证的固定路径
- 不捏造健康端点
- GUI、交互式登录、sudo 安装等非 Agent 可控动作必须标记为人工动作

<!-- sourceHash: 613fa5f8b0f2c56a98a2228d4d03ddc4732847cbe6316d74b93f25a6413d1494 -->
