# 项目情况

Template Contract: xian-harness/docs/project-situation

## 当前识别

| 标志 | 类型 | 状态 |
|---|---|---|
| `install.sh` | POSIX shell installer | active |
| `hooks/` | Git hook enforcement | active |
| `SKILL.md` | Agent delivery workflow | active |
| `tests/run_tests.sh` | shell regression entry | active |
| `.github/workflows/ci.yml` | macOS / Ubuntu CI | active |

## 运行时线索

| 运行时 | 证据 |
|---|---|
| Git + POSIX shell | installer 与全部 hooks |
| Perl | commit message Unicode 检查 |
| 无常驻服务 | 仓库没有服务启动入口、监听端口或数据库配置 |

## 边界

- 只支持目标 Git 仓库内的工程级安装。
- Installer 管理 Skills、prompt、references、policy example 和 hooks；目标工程 config 保持用户所有权。
- Git hooks 是客户端护栏，不能替代服务端权限、分支保护或质量门禁。
